---
layout: layouts/base.njk
title: Stories
description: From the trail, the farm, and everything in between.
---

<div class="stories-page">
  <div class="stories-header">
    <h1 class="stories-title">Stories</h1>
    <p class="stories-subtitle">From the trail, the farm, and everything in between.</p>
  </div>

  <div class="stories-list">
    {% for post in collections.posts %}
      <div class="story-row">
        <span class="story-date">
          <time datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate }}</time>
        </span>
        <span class="story-title">
          <a href="{{ post.url }}">{{ post.data.title }}</a>
        </span>
        <span class="story-tag">
          {% for tag in post.data.tags %}
            {% if tag != "post" and tag != "posts" %}{{ tag }}{% if not loop.last %}, {% endif %}{% endif %}
          {% endfor %}
        </span>
      </div>
    {% else %}
      <p style="font-family: var(--font-mono); color: var(--smoke); font-style: italic; font-size: 0.85rem; padding: 2rem 0;">
        The first story is coming. Check back soon.
      </p>
    {% endfor %}
  </div>
</div>
